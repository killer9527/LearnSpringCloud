spring:
  application:
    name: service-hello-world
  cloud:
    config:
      label: master
      profile: dev
      # uri: http://localhost:8760
      discovery:
        enabled: true
        service-id: config-server